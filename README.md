# minimal-login-interface
Clean and responsive login interface built with HTML, CSS, and Vanilla JS. It features a custom toggle switch, sharp SVG icons, and a centered card layout. I focused on writing simple, readable code and ensuring a smooth user experience across all devices without using heavy libraries.
